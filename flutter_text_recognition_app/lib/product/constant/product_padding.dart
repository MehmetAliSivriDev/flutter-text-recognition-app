import 'package:flutter/material.dart';

class ProductPadding extends EdgeInsets {
  //All
  const ProductPadding.allMoreLow() : super.all(10);
  const ProductPadding.allLow() : super.all(15);
  const ProductPadding.allMedium() : super.all(20);
  const ProductPadding.allHigh() : super.all(25);

  //Bottom
  const ProductPadding.bottomLow() : super.all(10);
}
